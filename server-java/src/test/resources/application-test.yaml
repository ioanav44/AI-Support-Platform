spring:
  application:
    name: pulseai-server-test
  
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: false
  
  datasource:
    url: jdbc:postgresql://localhost:5432/pulseai_test_db
    username: pulseai_admin
    password: pulseai_secret_pass
    driver-class-name: org.postgresql.Driver

server:
  port: 8080

jwt:
  secret: test_secret_key_for_testing_only_9Mjkl@pQrStU2VwXyZa1bCdEfGhIjKlMn
  expiration:
    access-token: 900000
    refresh-token: 604800000

logging:
  level:
    root: WARN
    com.pulseai: DEBUG
